<form method="post" action="{{ path('app_cart_remove', {'idProduct': cartItems.id[i]}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cartItems.id[i]) }}">
    <button class="btn btn-dark mt-2">Retirer du panier</button>
    
</form>
